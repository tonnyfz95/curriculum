{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome with HTML file",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}